{  
  "compilerOptions": {
    "target": "ES2015",           // Typescript를 ES2015버전의 Javascript로 변환
    "module": "ESNext",           // ECMA스크립트의 가장 최신 버전으로 모듈 사용, ECMA스크립트는 자바스크립트의 표준 명칭
    "moduleResolution": "Node",   // 모듈의 해석방식
    "esModuleInterop": true,      // ES6 모듈 사양을 준수하여 CommonJS 모듈을 가져올 수 있게 된다
    "lib": ["ESNext", "DOM"],     // 타입스크립트가 자바스크립트로 컴파일 할 때 내부적으로 사용을 해야하는 라이브러리 목록을 지정할 수 있다.
    "strict": true,               // 타입스크립트의 문법을 엄격하게 사용
  },
  "include": [      // 컴파일 할 경로
    "src/**/*.ts"   // src 폴더와 src 폴더 안의 모든 경로에서 모든 타입스크립트 파일
  ],
  "exclude": [      // 컴파일 제외할 경로 또는 파일
    "node_modules"
  ]        
}